# Mosquitto Configuration for ESP32 IoT Project
# 允許匿名連線（開發用）
allow_anonymous true

# TCP Listener (給 ESP32 使用)
listener 1883
protocol mqtt

# WebSocket Listener (給網頁使用)
listener 9001
protocol websockets

# 日誌設定
log_dest stdout
log_type all
connection_messages true
log_timestamp true
